import Route from "@ember/routing/route";

export default class AdminPluginsShowDiscourseTopicViewModesRoute extends Route {
  // Add model/afterModel if you need data; otherwise this can stay empty
}
